---
import type { Frontmatter } from "../config";

const {
	frontmatter: { title },
} = Astro.props as {
	frontmatter: Frontmatter;
};
---

<article class="prose dark:prose-invert max-w-none col-span-2 pt-10">
	<h1>{title}</h1>
	<slot />
</article>
